# Student-Course-Management-System
The Student Course Management System is a Spring Boot web application for managing students, courses, and enrollments. It uses Spring MVC, JSP/JSTL for UI, Spring Data JPA for persistence, and MySQL as the database. Admins can add, update, view, and delete records while maintaining relationships.
